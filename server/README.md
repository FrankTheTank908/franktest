# NetProof server

Fastify service for NetProof throughput and diagnostics endpoints.

## Run

```bash
npm install
npm run dev
```

## Docker

```bash
docker build -t netproof-server .
docker run -p 8080:8080 netproof-server
```
