import SwiftUI

struct PaywallView: View {
    var body: some View {
        VStack(spacing: 10) {
            Text("NetProof Premium").font(.title2).bold()
            Text("Unlock unlimited history, PDF exports, and advanced tests.")
        }.padding()
    }
}
